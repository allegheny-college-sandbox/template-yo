on: [push]

jobs:
  hello_arborist:
    runs-on: ubuntu-latest
    name: Introduce the Arborist!
    steps:
      - name: Arborist
        id: arborist
        uses: term-world/arborist@v0.50.2
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
